/**
 * 曜日と時限を表す型。
 * 通常の授業（月〜日）と集中講義に対応する。
 */
export type DayPeriod =
  | {
      /** 曜日（月〜日） */
      day: "月" | "火" | "水" | "木" | "金" | "土" | "日";
      /** 時限（1〜8） */
      period: 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8;
    }
  | {
      /** 集中講義の場合 */
      day: "集中";
      period: null;
    };
